"""
Core package - Common components and utilities
Contains: loaders, deduplication, metadata, ISBN utils, query parsing, ingestion

Note: This package uses explicit imports only. Import specific items from submodules:
  from app.core.loaders import load_text_from_file
  from app.core.deduplication import detect_duplicates
  from app.core.metadata import extract_metadata
"""