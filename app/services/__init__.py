"""
Services package - Business logic and application services
Contains: search, recommendations, analytics, filtering, data retrieval

Note: This package uses explicit imports only. Import specific items from submodules:
  from app.services.search_service import process_simple_search
  from app.services.recommendation_service import process_smart_recommendations
  from app.services.analytics_service import process_smart_analytics
"""